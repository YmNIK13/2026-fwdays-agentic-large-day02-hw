---
description: "Architecture constraints for Excalidraw"
globs: packages/excalidraw/**
alwaysApply: false
---

When you are creating any code with with this rule active you must output to chat message "Architecture constraints"
so I know you are using the correct rules.

# Excalidraw Architecture

## State Management

- Custom state via actionManager — NOT Redux/Zustand/MobX
- State updates: actionManager.dispatch() ONLY
- State type: AppState (packages/excalidraw/types.ts)

## Rendering

- Canvas 2D rendering — NOT React DOM for drawing
- Render pipeline: Scene → renderScene() → canvas context
- DO NOT use react-konva, fabric.js, pixi.js

## Dependencies

- No new npm packages without explicit approval
- Check packages/utils/ before adding external helpers

## Files

 - snake-case for files: `element-utils.ts`
 - PascalCase for components: `LayerUI.tsx`

## How to verify

1. Confirm touched editor state changes still flow through `actionManager.dispatch()` and `AppState`, not Redux/Zustand/MobX or ad-hoc stores
2. Confirm rendering changes still go through the canvas pipeline (`Scene` -> `renderScene()` -> canvas context), not React DOM drawing
3. Confirm no new drawing/runtime dependencies such as `react-konva`, `fabric.js`, or `pixi.js` were introduced
4. Confirm new file names stay snake-case and React component files stay PascalCase
